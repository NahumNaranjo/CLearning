#ifndef INFILE_H
#define INFILE_H
    #include <cfm.h>
    #include <ariadne.h>
    typedef struct{
        char* title;
        char* dev;
        char* genre;
        char* verbs;
        char* nouns;
        char* adjectives;
        char* script;
        char* characters;
    } programInfo;
#endif