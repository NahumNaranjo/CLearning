#include "hub.h"
#include <string.h>
#include <stdlib.h>

void gamesInterpreter(char* choice){
    switch (*choice) {
        default:
            break;
    }
}

void toolsInterpreter(char* choice){
    switch (*choice) {
        default:
            break;
    }
}